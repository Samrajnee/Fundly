{
  "name": "fundly",
  "private": true, "scripts": {
    "dev": "turbo run dev --filter=web --filter=server",
    "dev:web": "turbo run dev --filter=web",
    "dev:server": "turbo run dev --filter=server",
    "build": "turbo run build --filter=web --filter=server",
    "lint": "turbo run lint --filter=web --filter=server",
    "type-check": "turbo run type-check --filter=web --filter=server"
  },

  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "packageManager": "pnpm@9.0.0"
}